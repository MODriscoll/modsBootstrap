layout (std140, binding = 0) uniform Camera
{
	mat4 projection;		// Projection of scene
	mat4 view;				// View of scene
	vec3 position;			// Position of camera (in world space)
	vec3 heading;			// Heading of camera (in world space)
	mat4 ortho;				// Orthographic view of screen
};